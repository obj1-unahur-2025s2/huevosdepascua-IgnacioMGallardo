import huevosDePascua.*
import chicos.*
import caceriaDeHuevos.*

describe "group of tests" {

  method initialize() {caceria.inicializarJuego()}
  test "El de mayor cantidad de calorias es la Marioshka" {
    assert.equals(matrioshka, caceria.noEncontradoDeMayorCalorias())
  }
  test "test 2" {
    assert.that(true)
  }

}